---
import { tagSlug } from "../lib/tag-slug.js";
import { humanizeLabel } from "../lib/content";

type Props = {
  tags: string[];
  label?: string;
};

const { tags, label = "Tags" } = Astro.props;
const uniqueTags = [...new Set(tags)];
---

{
  uniqueTags.length > 0 ? (
    <ul class="tag-list" aria-label={label}>
      {uniqueTags.map((tag) => (
        <li>
          <a href={`/tags/${tagSlug(tag)}/`}>{humanizeLabel(tag)}</a>
        </li>
      ))}
    </ul>
  ) : null
}
